name: Deploy Worker

on:
  # Production promotion is deliberate and requires the team leader's approval;
  # main pushes only run CI.
  workflow_dispatch:
    inputs:
      deploy_confirm:
        description: "Confirm that the reviewed RLS/build/readiness evidence is for this SHA"
        required: true
        type: boolean
        default: false

permissions:
  contents: read

concurrency:
  group: deploy-worker-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rls:
    runs-on: ubuntu-latest
    steps:
      - name: Require main branch
        if: ${{ github.ref != 'refs/heads/main' }}
        run: |
          echo "::error::Production verification is allowed only from refs/heads/main (received: $GITHUB_REF)."
          exit 1
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22
          cache: npm
      - uses: supabase/setup-cli@ab058987d8d6c725971f6cf9d0b5c98467e30bd1 # v1.7.1
        with:
          version: 2.109.1
      - run: npm ci
      - name: Start local Supabase with Stage B held back
        run: |
          stage_b_migration="supabase/migrations/20260718073243_finalize_review_workflow_hardening.sql"
          follow_up_migration="supabase/migrations/20260718123250_remove_obsolete_review_status_rpc.sql"
          held_migration="$RUNNER_TEMP/$(basename "$stage_b_migration")"
          held_follow_up_migration="$RUNNER_TEMP/$(basename "$follow_up_migration")"
          restore_stage_b() {
            if [ -f "$held_migration" ]; then
              mv "$held_migration" "$stage_b_migration"
            fi
            if [ -f "$held_follow_up_migration" ]; then
              mv "$held_follow_up_migration" "$follow_up_migration"
            fi
          }
          trap restore_stage_b EXIT
          mv "$stage_b_migration" "$held_migration"
          mv "$follow_up_migration" "$held_follow_up_migration"
          supabase start
          restore_stage_b
          trap - EXIT
      - name: Remove disposable local attachment bucket through Storage API
        run: |
          eval "$(supabase status -o env)"
          SUPABASE_URL="$API_URL" \
          SUPABASE_SERVICE_ROLE_KEY="$SERVICE_ROLE_KEY" \
            node scripts/purge-review-attachments.mjs --execute --confirm=PURGE_REVIEW_ATTACHMENTS
      - name: Apply Stage B and later migrations
        run: supabase migration up --local
      - name: Seed deterministic RLS fixtures
        run: |
          eval "$(supabase status -o env)"
          export SUPABASE_URL="$API_URL"
          export SUPABASE_ANON_KEY="$ANON_KEY"
          export SUPABASE_SERVICE_ROLE_KEY="$SERVICE_ROLE_KEY"
          {
            echo "SUPABASE_URL=$SUPABASE_URL"
            echo "SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY"
            echo "SUPABASE_SERVICE_ROLE_KEY=$SUPABASE_SERVICE_ROLE_KEY"
            echo "SUPABASE_DB_URL=$DB_URL"
            node scripts/setup-rls-fixtures.mjs
          } >> "$GITHUB_ENV"
      - run: npm run test:rls
      - name: Verify private status audit event
        run: |
          count=$(psql "$SUPABASE_DB_URL" -tAc "select count(*) from private.review_status_events")
          [ "$count" -ge 1 ] || { echo "::error::RLS status RPC produced no private audit event"; exit 1; }
          mutation_count=$(psql "$SUPABASE_DB_URL" -tAc "select count(*) from private.audit_events")
          [ "$mutation_count" -ge 1 ] || { echo "::error::RLS fixtures produced no private mutation audit event"; exit 1; }
      - if: ${{ always() }}
        run: supabase stop --no-backup

  deploy:
    needs: rls
    runs-on: ubuntu-latest
    environment: production
    # 시크릿은 job 레벨에 두지 않는다 — npm ci/test가 실행하는 서드파티 코드에
    # 노출되지 않도록 실제로 쓰는 단계(Build, Deploy)에만 주입한다.
    env:
      CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
      WORKER_NAME: ${{ vars.WORKER_NAME }}
      WORKER_URL: ${{ vars.WORKER_URL }}
      VITE_SUPABASE_URL: ${{ vars.VITE_SUPABASE_URL }}

    steps:
      - name: Require main branch
        if: ${{ github.ref != 'refs/heads/main' }}
        run: |
          echo "::error::Production deployment is allowed only from refs/heads/main (received: $GITHUB_REF)."
          exit 1

      - name: Require explicit deployment confirmation
        if: ${{ inputs.deploy_confirm != true }}
        run: |
          echo "::error::Production deployment requires deploy_confirm=true after reviewing the RLS and DB readiness evidence."
          exit 1

      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Run tests
        run: npm test

      - name: Check deploy configuration
        env:
          CF_TOKEN_SET: ${{ secrets.CLOUDFLARE_API_TOKEN != '' }}
          ANON_KEY_SET: ${{ secrets.VITE_SUPABASE_ANON_KEY != '' }}
          DB_URL_SET: ${{ secrets.SUPABASE_DB_URL != '' }}
        run: |
          missing=()
          if [ "$CF_TOKEN_SET" != "true" ]; then
            missing+=("CLOUDFLARE_API_TOKEN (secret)")
          fi
          if [ -z "$CLOUDFLARE_ACCOUNT_ID" ]; then
            missing+=("CLOUDFLARE_ACCOUNT_ID (variable)")
          fi
          if [ -z "$WORKER_NAME" ]; then
            missing+=("WORKER_NAME (variable)")
          fi
          if [ -z "$WORKER_URL" ]; then
            missing+=("WORKER_URL (variable; post-deploy health check)")
          fi
          if [ -z "$VITE_SUPABASE_URL" ]; then
            missing+=("VITE_SUPABASE_URL (variable)")
          fi
          if [ "$ANON_KEY_SET" != "true" ]; then
            missing+=("VITE_SUPABASE_ANON_KEY (secret)")
          fi
          if [ "$DB_URL_SET" != "true" ]; then
            missing+=("SUPABASE_DB_URL (secret; deploy DB-readiness gate)")
          fi

          if [ "${#missing[@]}" -gt 0 ]; then
            {
              echo "## Deploy configuration incomplete"
              echo ""
              echo "The following GitHub Actions settings are missing:"
              echo ""
              for item in "${missing[@]}"; do
                echo "- \`$item\`"
              done
              echo ""
              echo "**Result:** deployment was **blocked and this workflow failed**. No production deploy occurred."
              echo "Register the missing Variables/Secrets under Repository Settings > Secrets and variables > Actions, then rerun the confirmed manual deployment."
            } >> "$GITHUB_STEP_SUMMARY"

            echo "::error::Deploy configuration is incomplete; refusing a green no-op production run. See job summary."
            exit 1
          fi

      - name: Verify production DB readiness
        env:
          DATABASE_URL: ${{ secrets.SUPABASE_DB_URL }}
        run: |
          assignment_ready=$(psql "$DATABASE_URL" -tAc \
            "select
              exists (select 1 from supabase_migrations.schema_migrations where version = '202607110001')
              and exists (select 1 from supabase_migrations.schema_migrations where version = '202607110002')
              and exists (select 1 from supabase_migrations.schema_migrations where version = '202607110003')
              and exists (select 1 from supabase_migrations.schema_migrations where version = '202607110004')
              and exists (select 1 from supabase_migrations.schema_migrations where version = '202607110005')
              and exists (select 1 from supabase_migrations.schema_migrations where version = '202607110006')
              and exists (select 1 from supabase_migrations.schema_migrations where version = '202607110007')
              and exists (select 1 from supabase_migrations.schema_migrations where version = '202607110008')
              and exists (select 1 from supabase_migrations.schema_migrations where version = '202607110009')
              and exists (select 1 from supabase_migrations.schema_migrations where version = '202607110010')
              and exists (select 1 from supabase_migrations.schema_migrations where version = '202607110011')
              and not has_table_privilege('authenticated', 'public.project_assignments', 'INSERT')
              and not has_table_privilege('authenticated', 'public.project_assignments', 'UPDATE')
              and not has_table_privilege('authenticated', 'public.project_assignments', 'DELETE')
              and to_regprocedure('public.bump_project_revision_from_assignment()') is not null
              and coalesce((select prosecdef from pg_proc where oid = to_regprocedure('public.bump_project_revision_from_assignment()')), false)
              and coalesce((select exists (select 1 from unnest(proconfig) cfg where cfg in ('search_path=' || chr(34) || chr(34), 'search_path=')) from pg_proc where oid = to_regprocedure('public.bump_project_revision_from_assignment()')), false)
              and coalesce((select proacl is not null and proacl::text not like '%{=X%' and proacl::text not like '%,=X%' from pg_proc where oid = to_regprocedure('public.bump_project_revision_from_assignment()')), false)
              and not coalesce(has_function_privilege('authenticated', to_regprocedure('public.bump_project_revision_from_assignment()'), 'EXECUTE'), true)
              and not coalesce(has_function_privilege('anon', to_regprocedure('public.bump_project_revision_from_assignment()'), 'EXECUTE'), true)
              and exists (select 1 from pg_trigger where tgrelid = 'public.project_assignments'::regclass and tgname = 'project_assignments_bump_project_revision' and tgenabled in ('O', 'A'))
              and not exists (select 1 from pg_policies where schemaname = 'public' and tablename in ('review_requests', 'review_feedback') and cmd in ('INSERT', 'UPDATE', 'DELETE', 'ALL'))
              and not has_table_privilege('authenticated', 'public.review_requests', 'INSERT')
              and not has_table_privilege('authenticated', 'public.review_requests', 'UPDATE')
              and not has_table_privilege('authenticated', 'public.review_requests', 'DELETE')
              and not has_table_privilege('authenticated', 'public.review_feedback', 'INSERT')
              and not has_table_privilege('authenticated', 'public.review_feedback', 'UPDATE')
              and not has_table_privilege('authenticated', 'public.review_feedback', 'DELETE')
              and coalesce(pg_get_functiondef(to_regprocedure('public.is_active_leader()')) like '%password_is_current()%', false)
              and to_regprocedure('public.reopen_review_request(uuid,timestamp with time zone)') is not null
              and coalesce((select prosecdef from pg_proc where oid = to_regprocedure('public.reopen_review_request(uuid,timestamp with time zone)')), false)
              and coalesce(has_function_privilege('authenticated', to_regprocedure('public.reopen_review_request(uuid,timestamp with time zone)'), 'EXECUTE'), false)
              and not coalesce(has_function_privilege('anon', to_regprocedure('public.reopen_review_request(uuid,timestamp with time zone)'), 'EXECUTE'), true)
              and not coalesce(has_function_privilege('authenticated', to_regprocedure('public.reopen_review_request(uuid)'), 'EXECUTE'), false)
              and to_regprocedure('public.add_product_assignment(uuid,uuid)') is not null
              and to_regprocedure('public.add_duty_assignment(uuid,uuid)') is not null
              and coalesce((select prosecdef from pg_proc where oid = to_regprocedure('public.add_product_assignment(uuid,uuid)')), false)
              and coalesce((select exists (select 1 from unnest(proconfig) cfg where cfg in ('search_path=' || chr(34) || chr(34), 'search_path=')) from pg_proc where oid = to_regprocedure('public.add_product_assignment(uuid,uuid)')), false)
              and not coalesce(has_function_privilege('anon', to_regprocedure('public.add_product_assignment(uuid,uuid)'), 'EXECUTE'), true)
              and coalesce((select prosecdef from pg_proc where oid = to_regprocedure('public.add_duty_assignment(uuid,uuid)')), false)
              and coalesce((select exists (select 1 from unnest(proconfig) cfg where cfg in ('search_path=' || chr(34) || chr(34), 'search_path=')) from pg_proc where oid = to_regprocedure('public.add_duty_assignment(uuid,uuid)')), false)
              and not coalesce(has_function_privilege('anon', to_regprocedure('public.add_duty_assignment(uuid,uuid)'), 'EXECUTE'), true)
              and coalesce(has_function_privilege('authenticated', to_regprocedure('public.add_product_assignment(uuid,uuid)'), 'EXECUTE'), false)
              and coalesce(has_function_privilege('authenticated', to_regprocedure('public.add_duty_assignment(uuid,uuid)'), 'EXECUTE'), false)
              and to_regprocedure('public.replace_project_assignments_if_current(uuid,uuid[],timestamp with time zone)') is not null
              and coalesce((select pg_get_function_result(to_regprocedure('public.replace_project_assignments_if_current(uuid,uuid[],timestamp with time zone)')) = 'timestamp with time zone'), false)
              and coalesce((select prosecdef from pg_proc where oid = to_regprocedure('public.replace_project_assignments_if_current(uuid,uuid[],timestamp with time zone)')), false)
              and coalesce((select exists (select 1 from unnest(proconfig) cfg where cfg in ('search_path=' || chr(34) || chr(34), 'search_path=')) from pg_proc where oid = to_regprocedure('public.replace_project_assignments_if_current(uuid,uuid[],timestamp with time zone)')), false)
              and coalesce(has_function_privilege('authenticated', to_regprocedure('public.replace_project_assignments_if_current(uuid,uuid[],timestamp with time zone)'), 'EXECUTE'), false)
              and not coalesce(has_function_privilege('anon', to_regprocedure('public.replace_project_assignments_if_current(uuid,uuid[],timestamp with time zone)'), 'EXECUTE'), true)
              and to_regprocedure('public.count_active_leaders_except(uuid)') is not null
              and coalesce((select prosecdef from pg_proc where oid = to_regprocedure('public.count_active_leaders_except(uuid)')), false)
              and coalesce((select provolatile = 'v' from pg_proc where oid = to_regprocedure('public.count_active_leaders_except(uuid)')), false)
              and coalesce((select exists (select 1 from unnest(proconfig) cfg where cfg in ('search_path=' || chr(34) || chr(34), 'search_path=')) from pg_proc where oid = to_regprocedure('public.count_active_leaders_except(uuid)')), false)
              and not coalesce(has_function_privilege('authenticated', to_regprocedure('public.count_active_leaders_except(uuid)'), 'EXECUTE'), true)
              and not coalesce(has_function_privilege('anon', to_regprocedure('public.count_active_leaders_except(uuid)'), 'EXECUTE'), true)
              and pg_get_functiondef(to_regprocedure('public.guard_last_active_leader_profile()')) like '%pg_advisory_xact_lock%'
              and pg_get_functiondef(to_regprocedure('public.guard_last_active_leader_allowed_user()')) like '%pg_advisory_xact_lock%'")

          if [ "$assignment_ready" != "t" ]; then
            echo "::error::Production DB is not ready for this frontend. Apply and verify migrations 202607110001 through 202607110011 first."
            exit 1
          fi

          leader_ui_ready=$(psql "$DATABASE_URL" -tAc \
            "select
              exists (select 1 from supabase_migrations.schema_migrations where version = '20260714075451')
              and exists (select 1 from pg_attribute where attrelid = 'public.products'::regclass and attname = 'unassigned_reason' and not attisdropped)
              and exists (select 1 from pg_constraint where conrelid = 'public.products'::regclass and conname = 'products_unassigned_reason_length_check' and convalidated)
              and to_regprocedure('public.replace_product_assignments_with_reason(uuid,uuid[],text)') is not null
              and coalesce((select prosecdef from pg_proc where oid = to_regprocedure('public.replace_product_assignments_with_reason(uuid,uuid[],text)')), false)
              and coalesce((select exists (select 1 from unnest(proconfig) cfg where cfg in ('search_path=' || chr(34) || chr(34), 'search_path=')) from pg_proc where oid = to_regprocedure('public.replace_product_assignments_with_reason(uuid,uuid[],text)')), false)
              and coalesce(has_function_privilege('authenticated', to_regprocedure('public.replace_product_assignments_with_reason(uuid,uuid[],text)'), 'EXECUTE'), false)
              and not coalesce(has_function_privilege('anon', to_regprocedure('public.replace_product_assignments_with_reason(uuid,uuid[],text)'), 'EXECUTE'), true)
              and to_regprocedure('public.clear_product_unassigned_reason()') is not null
              and coalesce((select prosecdef from pg_proc where oid = to_regprocedure('public.clear_product_unassigned_reason()')), false)
              and not coalesce(has_function_privilege('authenticated', to_regprocedure('public.clear_product_unassigned_reason()'), 'EXECUTE'), true)
              and not coalesce(has_function_privilege('anon', to_regprocedure('public.clear_product_unassigned_reason()'), 'EXECUTE'), true)
              and exists (select 1 from pg_trigger where tgrelid = 'public.product_assignments'::regclass and tgname = 'product_assignments_clear_unassigned_reason' and tgfoid = to_regprocedure('public.clear_product_unassigned_reason()') and tgenabled in ('O', 'A'))
              and to_regprocedure('public.validate_project_assignment_member()') is not null
              and coalesce((select prosecdef from pg_proc where oid = to_regprocedure('public.validate_project_assignment_member()')), false)
              and coalesce((select exists (select 1 from unnest(proconfig) cfg where cfg in ('search_path=' || chr(34) || chr(34), 'search_path=')) from pg_proc where oid = to_regprocedure('public.validate_project_assignment_member()')), false)
              and not coalesce(has_function_privilege('authenticated', to_regprocedure('public.validate_project_assignment_member()'), 'EXECUTE'), true)
              and not coalesce(has_function_privilege('anon', to_regprocedure('public.validate_project_assignment_member()'), 'EXECUTE'), true)
              and coalesce(pg_get_functiondef(to_regprocedure('public.validate_project_assignment_member()')) like '%new.user_id = auth.uid()%', false)
              and coalesce(pg_get_functiondef(to_regprocedure('public.validate_project_assignment_member()')) like '%profile_has_role(new.user_id, ''member''%', false)
              and coalesce(pg_get_functiondef(to_regprocedure('public.validate_project_assignment_member()')) like '%is_active_leader()%', false)")
          if [ "$leader_ui_ready" != "t" ]; then
            echo "::error::Production DB product-reason/current-leader assignment contract is not ready"
            exit 1
          fi

          leader_policy_ready=$(psql "$DATABASE_URL" -tAc \
            "select not exists (
              select 1 from pg_policies
               where schemaname = 'public'
                 and (coalesce(qual, '') like '%is_leader()%' or coalesce(with_check, '') like '%is_leader()%')
                 and coalesce(qual, '') not like '%is_active_leader()%'
                 and coalesce(with_check, '') not like '%is_active_leader()%'
            )")
          if [ "$leader_policy_ready" != "t" ]; then
            echo "::error::Production DB has a role-only leader RLS policy; active/password gate is not ready"
            exit 1
          fi

          announcements_ready=$(psql "$DATABASE_URL" -tAc \
            "select
              exists (select 1 from supabase_migrations.schema_migrations where version = '20260716200422')
              and to_regclass('public.announcements') is not null
              and coalesce((select relrowsecurity from pg_class where oid = to_regclass('public.announcements')), false)
              and exists (select 1 from pg_constraint where conrelid = to_regclass('public.announcements') and conname = 'announcements_title_length_check' and convalidated)
              and exists (select 1 from pg_constraint where conrelid = to_regclass('public.announcements') and conname = 'announcements_body_length_check' and convalidated)
              and exists (select 1 from pg_constraint where conrelid = to_regclass('public.announcements') and conname = 'announcements_pin_state_check' and convalidated)
              and exists (select 1 from pg_class where oid = to_regclass('public.announcements_created_by_idx') and relkind = 'i')
              and exists (select 1 from pg_class where oid = to_regclass('public.announcements_board_order_idx') and relkind = 'i')
              and exists (select 1 from pg_trigger where tgrelid = to_regclass('public.announcements') and tgname = 'announcements_enforce_invariants' and tgfoid = to_regprocedure('private.enforce_announcement_invariants()') and tgenabled in ('O', 'A'))
              and exists (select 1 from pg_trigger where tgrelid = to_regclass('public.announcements') and tgname = 'announcements_set_updated_at' and tgfoid = to_regprocedure('public.set_updated_at()') and tgenabled in ('O', 'A'))
              and exists (select 1 from pg_trigger where tgrelid = to_regclass('public.announcements') and tgname = 'announcements_private_audit' and tgfoid = to_regprocedure('private.record_mutation_audit()') and tgenabled in ('O', 'A'))
              and to_regprocedure('private.enforce_announcement_invariants()') is not null
              and not coalesce((select prosecdef from pg_proc where oid = to_regprocedure('private.enforce_announcement_invariants()')), true)
              and coalesce((select 'search_path=pg_catalog, public, private, pg_temp' = any(proconfig) from pg_proc where oid = to_regprocedure('private.enforce_announcement_invariants()')), false)
              and not coalesce(has_function_privilege('authenticated', to_regprocedure('private.enforce_announcement_invariants()'), 'EXECUTE'), true)
              and not coalesce(has_function_privilege('anon', to_regprocedure('private.enforce_announcement_invariants()'), 'EXECUTE'), true)
              and coalesce(pg_get_functiondef(to_regprocedure('private.enforce_announcement_invariants()')) like '%new.title := btrim(new.title)%', false)
              and coalesce(pg_get_functiondef(to_regprocedure('private.enforce_announcement_invariants()')) like '%new.created_by is distinct from old.created_by%', false)
              and coalesce(pg_get_functiondef(to_regprocedure('private.enforce_announcement_invariants()')) like '%new.created_at is distinct from old.created_at%', false)
              and coalesce(pg_get_functiondef(to_regprocedure('private.enforce_announcement_invariants()')) like '%new.pinned_at := now()%', false)
              and coalesce(has_table_privilege('authenticated', to_regclass('public.announcements'), 'SELECT'), false)
              and coalesce(has_table_privilege('authenticated', to_regclass('public.announcements'), 'DELETE'), false)
              and not coalesce(has_table_privilege('authenticated', to_regclass('public.announcements'), 'INSERT'), true)
              and not coalesce(has_table_privilege('authenticated', to_regclass('public.announcements'), 'UPDATE'), true)
              and coalesce(has_column_privilege('authenticated', to_regclass('public.announcements'), 'title', 'INSERT'), false)
              and coalesce(has_column_privilege('authenticated', to_regclass('public.announcements'), 'body', 'INSERT'), false)
              and coalesce(has_column_privilege('authenticated', to_regclass('public.announcements'), 'is_pinned', 'INSERT'), false)
              and coalesce(has_column_privilege('authenticated', to_regclass('public.announcements'), 'title', 'UPDATE'), false)
              and coalesce(has_column_privilege('authenticated', to_regclass('public.announcements'), 'body', 'UPDATE'), false)
              and coalesce(has_column_privilege('authenticated', to_regclass('public.announcements'), 'is_pinned', 'UPDATE'), false)
              and not coalesce(has_column_privilege('authenticated', to_regclass('public.announcements'), 'pinned_at', 'INSERT'), true)
              and not coalesce(has_column_privilege('authenticated', to_regclass('public.announcements'), 'pinned_at', 'UPDATE'), true)
              and not coalesce(has_column_privilege('authenticated', to_regclass('public.announcements'), 'created_by', 'INSERT'), true)
              and not coalesce(has_column_privilege('authenticated', to_regclass('public.announcements'), 'created_by', 'UPDATE'), true)
              and not coalesce(has_column_privilege('authenticated', to_regclass('public.announcements'), 'created_at', 'UPDATE'), true)
              and not coalesce(has_table_privilege('anon', to_regclass('public.announcements'), 'SELECT'), true)
              and not coalesce(has_table_privilege('anon', to_regclass('public.announcements'), 'INSERT'), true)
              and not coalesce(has_table_privilege('anon', to_regclass('public.announcements'), 'UPDATE'), true)
              and not coalesce(has_table_privilege('anon', to_regclass('public.announcements'), 'DELETE'), true)
              and exists (select 1 from pg_policies where schemaname = 'public' and tablename = 'announcements' and policyname = 'announcements_select_app_user' and qual like '%is_active_self()%' and qual not like '%can_use_app()%')
              and exists (select 1 from pg_policies where schemaname = 'public' and tablename = 'announcements' and policyname = 'announcements_insert_leader' and with_check like '%is_active_leader()%' and with_check like '%created_by%' and with_check like '%auth.uid()%')
              and exists (select 1 from pg_policies where schemaname = 'public' and tablename = 'announcements' and policyname = 'announcements_update_leader' and qual like '%is_active_leader()%' and with_check like '%is_active_leader()%')
              and exists (select 1 from pg_policies where schemaname = 'public' and tablename = 'announcements' and policyname = 'announcements_delete_leader' and qual like '%is_active_leader()%')")
          if [ "$announcements_ready" != "t" ]; then
            echo "::error::Production DB announcement board schema/RLS/grants/audit contract is not ready"
            exit 1
          fi

          review_contract_ready=$(psql "$DATABASE_URL" -tAc \
            "select
              has_table_privilege('authenticated', 'public.review_requests', 'SELECT')
              and not has_table_privilege('authenticated', 'public.review_requests', 'INSERT')
              and not has_table_privilege('authenticated', 'public.review_requests', 'UPDATE')
              and not has_table_privilege('authenticated', 'public.review_requests', 'DELETE')
              and not exists (select 1 from pg_attribute where attrelid = 'public.review_requests'::regclass and attname = 'attachment_url' and not attisdropped)
              and to_regclass('private.review_status_events') is not null
              and exists (
                select 1 from pg_trigger
                where tgrelid = 'public.review_requests'::regclass
                  and tgname = 'review_requests_record_status_event'
                  and tgenabled in ('O', 'A')
              )")
          if [ "$review_contract_ready" != "t" ]; then
            echo "::error::Production DB review status RPC/audit contract is not ready"
            exit 1
          fi

          review_hardening_ready=$(psql "$DATABASE_URL" -tAc \
            "select
              exists (select 1 from supabase_migrations.schema_migrations where version = '20260718054122')
              and exists (select 1 from supabase_migrations.schema_migrations where version = '20260718054124')
              and exists (select 1 from supabase_migrations.schema_migrations where version = '20260718054127')
              and exists (select 1 from supabase_migrations.schema_migrations where version = '20260718073243')
              and exists (select 1 from supabase_migrations.schema_migrations where version = '20260718123250')
              and exists (select 1 from pg_enum value join pg_type type on type.oid = value.enumtypid where type.typname = 'review_status' and value.enumlabel = 'withdrawn')
              and to_regclass('public.review_events') is not null
              and to_regclass('public.review_read_receipts') is not null
              and coalesce((select relrowsecurity from pg_class where oid = to_regclass('public.review_events')), false)
              and coalesce((select relrowsecurity from pg_class where oid = to_regclass('public.review_read_receipts')), false)
              and not exists (select 1 from storage.buckets where id = 'review-attachments')
              and not exists (select 1 from pg_policies where schemaname = 'storage' and tablename = 'objects' and policyname like 'review_attachments_%')
              and not exists (select 1 from pg_attribute where attrelid = 'public.review_requests'::regclass and attname = 'attachment_url' and not attisdropped)
              and to_regprocedure('public.withdraw_review_request(uuid,timestamp with time zone,text)') is not null
              and to_regprocedure('public.void_review_feedback(uuid,timestamp with time zone,text)') is not null
              and to_regprocedure('public.mark_review_seen(uuid)') is not null
              and has_function_privilege('authenticated', to_regprocedure('public.mark_review_seen(uuid)'), 'EXECUTE')
              and not has_function_privilege('anon', to_regprocedure('public.mark_review_seen(uuid)'), 'EXECUTE')
              and exists (select 1 from pg_trigger where tgrelid = 'auth.users'::regclass and tgname = 'on_auth_password_changed' and tgenabled in ('O', 'A'))
              and exists (select 1 from pg_trigger where tgrelid = 'public.profiles'::regclass and tgname = 'profiles_block_physical_delete' and tgenabled in ('O', 'A'))
              and to_regprocedure('public.mark_password_changed()') is null
              and not exists (select 1 from pg_proc function join pg_namespace namespace on namespace.oid = function.pronamespace where namespace.nspname = 'public' and function.proname in ('mark_password_changed', 'validate_review_attachment_url'))
              and not has_table_privilege('authenticated', 'public.review_feedback', 'INSERT')
              and not has_table_privilege('authenticated', 'public.review_feedback', 'UPDATE')
              and not has_table_privilege('authenticated', 'public.review_feedback', 'DELETE')
              and to_regprocedure('public.update_review_request_status(uuid,public.review_status)') is null
              and to_regprocedure('public.reject_review_request(uuid,text)') is null
              and to_regprocedure('public.reopen_review_request(uuid)') is null
              and to_regprocedure('public.resubmit_review_request(uuid,text)') is null
              and not has_schema_privilege('anon', 'public', 'USAGE')
              and has_schema_privilege('authenticated', 'public', 'USAGE')
              and not has_schema_privilege('authenticated', 'public', 'CREATE')
              and has_schema_privilege('service_role', 'public', 'USAGE')
              and not has_schema_privilege('service_role', 'public', 'CREATE')
              and not has_schema_privilege('authenticated', 'private', 'USAGE')
              and has_schema_privilege('service_role', 'private', 'USAGE')
              and not has_schema_privilege('service_role', 'private', 'CREATE')
              and not exists (select 1 from pg_class object where object.relnamespace = 'public'::regnamespace and object.relkind in ('r', 'p') and not object.relrowsecurity)
              and not exists (select 1 from pg_class object where object.relnamespace = 'public'::regnamespace and object.relkind in ('r', 'p', 'v', 'm') and (has_table_privilege('anon', object.oid, 'SELECT') or has_table_privilege('anon', object.oid, 'INSERT') or has_table_privilege('anon', object.oid, 'UPDATE') or has_table_privilege('anon', object.oid, 'DELETE')))
              and not exists (select 1 from pg_class object where object.relnamespace = 'public'::regnamespace and object.relkind = 'S' and (has_sequence_privilege('anon', object.oid, 'USAGE') or has_sequence_privilege('anon', object.oid, 'SELECT') or has_sequence_privilege('anon', object.oid, 'UPDATE')))
              and not exists (
                select 1
                  from pg_catalog.pg_proc function
                 where function.pronamespace = 'public'::pg_catalog.regnamespace
                   and pg_catalog.has_function_privilege('anon', function.oid, 'EXECUTE')
                   and not exists (
                     select 1
                       from pg_catalog.pg_depend dependency
                       join pg_catalog.pg_extension extension
                         on extension.oid = dependency.refobjid
                      where dependency.classid = 'pg_catalog.pg_proc'::pg_catalog.regclass
                        and dependency.objid = function.oid
                        and dependency.objsubid = 0
                        and dependency.refclassid = 'pg_catalog.pg_extension'::pg_catalog.regclass
                        and dependency.refobjsubid = 0
                        and dependency.deptype = 'e'
                   )
              )
              and exists (select 1 from pg_default_acl where defaclrole = 'postgres'::regrole and defaclnamespace = 0 and defaclobjtype = 'f' and defaclacl::text not like '%{=X%' and defaclacl::text not like '%,=X%' and defaclacl::text not like '%anon=X%' and defaclacl::text not like '%authenticated=X%' and defaclacl::text not like '%service_role=X%')
              and (select count(distinct defaclobjtype) = 3 from pg_default_acl where defaclrole = 'postgres'::regrole and defaclnamespace = 'public'::regnamespace and defaclobjtype in ('r', 'S', 'f'))
              and to_regprocedure('public.restore_product_change_scope(uuid,text)') is not null")
          if [ "$review_hardening_ready" != "t" ]; then
            echo "::error::Production DB review workflow Stage B cleanup/ACL contract is not ready"
            exit 1
          fi

          review_resubmission_ready=$(psql "$DATABASE_URL" -tAc \
            "select
              exists (select 1 from supabase_migrations.schema_migrations where version = '20260715013615')
              and exists (select 1 from pg_attribute where attrelid = 'public.review_requests'::regclass and attname = 'review_round' and attnotnull and not attisdropped)
              and exists (select 1 from pg_attribute where attrelid = 'public.review_requests'::regclass and attname = 'rejection_count' and attnotnull and not attisdropped)
              and exists (select 1 from pg_attribute where attrelid = 'public.review_requests'::regclass and attname = 'last_submitted_at' and attnotnull and not attisdropped)
              and exists (select 1 from pg_constraint where conrelid = 'public.review_requests'::regclass and conname = 'review_requests_review_round_check' and convalidated)
              and exists (select 1 from pg_constraint where conrelid = 'public.review_requests'::regclass and conname = 'review_requests_rejection_count_check' and convalidated)
              and exists (select 1 from pg_attribute where attrelid = 'public.review_feedback'::regclass and attname = 'author_role' and attnotnull and not attisdropped)
              and exists (select 1 from pg_trigger where tgrelid = 'public.review_feedback'::regclass and tgname = 'review_feedback_validate_leader' and tgfoid = to_regprocedure('public.validate_review_feedback_leader()') and tgenabled in ('O', 'A'))
              and not exists (select 1 from pg_policies where schemaname = 'public' and tablename = 'review_requests' and cmd in ('INSERT', 'UPDATE', 'DELETE', 'ALL'))
              and not has_column_privilege('authenticated', 'public.review_requests', 'review_round', 'UPDATE')
              and not has_column_privilege('authenticated', 'public.review_requests', 'rejection_count', 'UPDATE')
              and not has_column_privilege('authenticated', 'public.review_requests', 'last_submitted_at', 'UPDATE')
              and to_regprocedure('public.resubmit_review_request(uuid,timestamp with time zone,text)') is not null
              and coalesce((select prosecdef from pg_proc where oid = to_regprocedure('public.resubmit_review_request(uuid,timestamp with time zone,text)')), false)
              and coalesce((select exists (select 1 from unnest(proconfig) cfg where cfg in ('search_path=' || chr(34) || chr(34), 'search_path=')) from pg_proc where oid = to_regprocedure('public.resubmit_review_request(uuid,timestamp with time zone,text)')), false)
              and coalesce(has_function_privilege('authenticated', to_regprocedure('public.resubmit_review_request(uuid,timestamp with time zone,text)'), 'EXECUTE'), false)
              and not coalesce(has_function_privilege('anon', to_regprocedure('public.resubmit_review_request(uuid,timestamp with time zone,text)'), 'EXECUTE'), true)
              and not coalesce(has_function_privilege('authenticated', to_regprocedure('public.resubmit_review_request(uuid,text)'), 'EXECUTE'), false)
              and coalesce(pg_get_functiondef(to_regprocedure('public.resubmit_review_request(uuid,timestamp with time zone,text)')) like '%for update%', false)
              and coalesce(pg_get_functiondef(to_regprocedure('public.resubmit_review_request(uuid,timestamp with time zone,text)')) like '%p_expected_updated_at%', false)")
          if [ "$review_resubmission_ready" != "t" ]; then
            echo "::error::Production DB review resubmission/history contract is not ready"
            exit 1
          fi

          review_activity_ready=$(psql "$DATABASE_URL" -tAc \
            "select
              to_regprocedure('public.approve_review_request(uuid,timestamp with time zone)') is not null
              and to_regprocedure('public.reject_review_request(uuid,timestamp with time zone,text)') is not null
              and to_regprocedure('public.add_review_feedback(uuid,text)') is not null
              and coalesce((select exists (select 1 from unnest(proconfig) cfg where cfg in ('search_path=' || chr(34) || chr(34), 'search_path=')) from pg_proc where oid = to_regprocedure('public.approve_review_request(uuid,timestamp with time zone)')), false)
              and coalesce((select exists (select 1 from unnest(proconfig) cfg where cfg in ('search_path=' || chr(34) || chr(34), 'search_path=')) from pg_proc where oid = to_regprocedure('public.reject_review_request(uuid,timestamp with time zone,text)')), false)
              and coalesce((select exists (select 1 from unnest(proconfig) cfg where cfg in ('search_path=' || chr(34) || chr(34), 'search_path=')) from pg_proc where oid = to_regprocedure('public.add_review_feedback(uuid,text)')), false)
              and has_function_privilege('authenticated', to_regprocedure('public.approve_review_request(uuid,timestamp with time zone)'), 'EXECUTE')
              and has_function_privilege('authenticated', to_regprocedure('public.reject_review_request(uuid,timestamp with time zone,text)'), 'EXECUTE')
              and has_function_privilege('authenticated', to_regprocedure('public.add_review_feedback(uuid,text)'), 'EXECUTE')
              and not has_function_privilege('anon', to_regprocedure('public.approve_review_request(uuid,timestamp with time zone)'), 'EXECUTE')
              and not has_function_privilege('anon', to_regprocedure('public.reject_review_request(uuid,timestamp with time zone,text)'), 'EXECUTE')
              and not has_function_privilege('anon', to_regprocedure('public.add_review_feedback(uuid,text)'), 'EXECUTE')
              and to_regprocedure('public.update_review_request_status(uuid,public.review_status)') is null
              and to_regprocedure('public.reject_review_request(uuid,text)') is null
              and exists (select 1 from pg_trigger where tgrelid = 'public.review_requests'::regclass and tgname = 'review_requests_record_status_event' and tgenabled in ('O', 'A'))
              and exists (select 1 from pg_trigger where tgrelid = 'public.review_feedback'::regclass and tgname = 'review_feedback_record_event' and tgenabled in ('O', 'A'))")
          if [ "$review_activity_ready" != "t" ]; then
            echo "::error::Production DB transactional review activity-log contract is not ready"
            exit 1
          fi

          leader_view_ready=$(psql "$DATABASE_URL" -tAc \
            "select pg_get_viewdef('public.public_leader_profiles'::regclass, true) like '%can_use_app()%'")
          if [ "$leader_view_ready" != "t" ]; then
            echo "::error::Production DB leader profile access gate is not ready"
            exit 1
          fi

          private_audit_ready=$(psql "$DATABASE_URL" -tAc \
            "select
              to_regclass('private.audit_events') is not null
              and not has_schema_privilege('authenticated', 'private', 'USAGE')
              and coalesce((select prosecdef from pg_proc where oid = to_regprocedure('private.record_mutation_audit()')), false)
              and coalesce((select 'search_path=pg_catalog' = any(proconfig) from pg_proc where oid = to_regprocedure('private.record_mutation_audit()')), false)
              and (select count(*) >= 13 from pg_trigger
                   where tgfoid = to_regprocedure('private.record_mutation_audit()')
                     and tgenabled in ('O', 'A'))
              and exists (select 1 from pg_trigger
                   where tgrelid = 'public.profile_notes'::regclass
                     and tgname = 'profile_notes_private_audit'
                     and tgfoid = to_regprocedure('private.record_mutation_audit()')
                     and tgenabled in ('O', 'A'))
              and exists (select 1 from pg_trigger
                   where tgrelid = to_regclass('public.announcements')
                     and tgname = 'announcements_private_audit'
                     and tgfoid = to_regprocedure('private.record_mutation_audit()')
                     and tgenabled in ('O', 'A'))")
          if [ "$private_audit_ready" != "t" ]; then
            echo "::error::Production DB private mutation audit contract is not ready"
            exit 1
          fi

          change_applications_ready=$(psql "$DATABASE_URL" -tAc \
            "select
              exists (select 1 from supabase_migrations.schema_migrations where version = '202607170001')
              and exists (select 1 from supabase_migrations.schema_migrations where version = '20260717123840')
              and to_regclass('public.change_applications') is not null
              and to_regclass('public.change_action_items') is not null
              and to_regclass('public.product_change_tasks') is not null
              and coalesce((select relrowsecurity from pg_class where oid = to_regclass('public.change_applications')), false)
              and coalesce((select relrowsecurity from pg_class where oid = to_regclass('public.change_action_items')), false)
              and coalesce((select relrowsecurity from pg_class where oid = to_regclass('public.product_change_tasks')), false)
              and exists (select 1 from pg_constraint where conrelid = to_regclass('public.change_applications') and conname = 'change_applications_source_url_scheme_check' and convalidated)
              and exists (select 1 from pg_constraint where conrelid = to_regclass('public.change_applications') and conname = 'change_applications_archive_state_check' and convalidated)
              and exists (select 1 from pg_constraint where conrelid = to_regclass('public.change_applications') and conname = 'change_applications_archive_reason_length_check' and convalidated)
              and (select count(*) = 3 from information_schema.columns where table_schema = 'public' and table_name = 'change_applications' and column_name in ('archived_at', 'archived_by', 'archive_reason'))
              and exists (select 1 from pg_class where oid = to_regclass('public.change_applications_archived_at_idx') and relkind = 'i')
              and coalesce(has_table_privilege('authenticated', to_regclass('public.change_applications'), 'SELECT'), false)
              and coalesce(has_table_privilege('authenticated', to_regclass('public.change_action_items'), 'SELECT'), false)
              and coalesce(has_table_privilege('authenticated', to_regclass('public.product_change_tasks'), 'SELECT'), false)
              and not coalesce(has_table_privilege('authenticated', to_regclass('public.change_applications'), 'INSERT'), true)
              and not coalesce(has_table_privilege('authenticated', to_regclass('public.change_applications'), 'UPDATE'), true)
              and not coalesce(has_table_privilege('authenticated', to_regclass('public.change_applications'), 'DELETE'), true)
              and not coalesce(has_table_privilege('authenticated', to_regclass('public.change_action_items'), 'INSERT'), true)
              and not coalesce(has_table_privilege('authenticated', to_regclass('public.change_action_items'), 'UPDATE'), true)
              and not coalesce(has_table_privilege('authenticated', to_regclass('public.change_action_items'), 'DELETE'), true)
              and not coalesce(has_table_privilege('authenticated', to_regclass('public.product_change_tasks'), 'INSERT'), true)
              and not coalesce(has_table_privilege('authenticated', to_regclass('public.product_change_tasks'), 'UPDATE'), true)
              and not coalesce(has_table_privilege('authenticated', to_regclass('public.product_change_tasks'), 'DELETE'), true)
              and not coalesce(has_table_privilege('anon', to_regclass('public.change_applications'), 'SELECT'), true)
              and not coalesce(has_table_privilege('anon', to_regclass('public.change_action_items'), 'SELECT'), true)
              and not coalesce(has_table_privilege('anon', to_regclass('public.product_change_tasks'), 'SELECT'), true)
              and exists (select 1 from pg_policies where schemaname = 'public' and tablename = 'change_applications' and policyname = 'change_applications_select_app_user' and qual like '%published_at%' and qual like '%cancelled%' and qual like '%created_by%' and qual like '%is_active_leader()%' and qual not like '%draft%')
              and exists (select 1 from pg_policies where schemaname = 'public' and tablename = 'change_action_items' and policyname = 'change_action_items_select_app_user' and qual like '%published_at%' and qual like '%cancelled%' and qual like '%created_by%' and qual like '%is_active_leader()%' and qual not like '%draft%')
              and exists (select 1 from pg_policies where schemaname = 'public' and tablename = 'product_change_tasks' and policyname = 'product_change_tasks_select_relevant' and qual like '%published_at%' and qual like '%cancelled%' and qual like '%assignee_id%')
              and not exists (
                select 1
                  from unnest(array[
                    to_regprocedure('public.list_change_application_product_scope()'),
                    to_regprocedure('public.list_change_application_assignees()'),
                    to_regprocedure('public.save_change_application_draft(uuid,timestamp with time zone,text,public.change_application_source,text,text,text,date,public.change_action_kind,text,text,date,jsonb)'),
                    to_regprocedure('public.publish_change_application(uuid,timestamp with time zone,text,public.change_application_source,text,text,text,date,public.change_action_kind,text,text,date,jsonb)'),
                    to_regprocedure('public.complete_product_change_task(uuid,text,text)'),
                    to_regprocedure('public.mark_product_change_task_not_applicable(uuid,text,text)'),
                    to_regprocedure('public.reopen_product_change_task(uuid,text)'),
                    to_regprocedure('public.reassign_product_change_tasks(uuid[],uuid,text)'),
                    to_regprocedure('public.cancel_product_change_task(uuid,text)'),
                    to_regprocedure('public.cancel_change_application(uuid,text)'),
                    to_regprocedure('public.archive_change_application(uuid,text)'),
                    to_regprocedure('public.restore_change_application(uuid,text)'),
                    to_regprocedure('public.assign_product_and_transfer_change_tasks(uuid,uuid,boolean,text)')
                  ]) required_rpc(function_oid)
                 where function_oid is null
                    or not coalesce((select prosecdef from pg_proc where oid = function_oid), false)
                    or not coalesce((select exists (select 1 from unnest(proconfig) cfg where cfg in ('search_path=' || chr(34) || chr(34), 'search_path=')) from pg_proc where oid = function_oid), false)
                    or not coalesce(has_function_privilege('authenticated', function_oid, 'EXECUTE'), false)
                    or coalesce(has_function_privilege('anon', function_oid, 'EXECUTE'), true)
              )
              and not coalesce(has_function_privilege('authenticated', to_regprocedure('private.persist_change_application(uuid,timestamp with time zone,text,public.change_application_source,text,text,text,date,public.change_action_kind,text,text,date,jsonb,boolean)'), 'EXECUTE'), true)
              and not coalesce(has_function_privilege('anon', to_regprocedure('private.persist_change_application(uuid,timestamp with time zone,text,public.change_application_source,text,text,text,date,public.change_action_kind,text,text,date,jsonb,boolean)'), 'EXECUTE'), true)
              and coalesce(pg_get_functiondef(to_regprocedure('public.save_change_application_draft(uuid,timestamp with time zone,text,public.change_application_source,text,text,text,date,public.change_action_kind,text,text,date,jsonb)')) like '%p_change_application_id, p_expected_updated_at, p_change_number%', false)
              and coalesce(pg_get_functiondef(to_regprocedure('public.publish_change_application(uuid,timestamp with time zone,text,public.change_application_source,text,text,text,date,public.change_action_kind,text,text,date,jsonb)')) like '%p_change_application_id, p_expected_updated_at, p_change_number%', false)
              and coalesce(pg_get_functiondef(to_regprocedure('private.persist_change_application(uuid,timestamp with time zone,text,public.change_application_source,text,text,text,date,public.change_action_kind,text,text,date,jsonb,boolean)')) like '%source url must use http or https%', false)
              and coalesce(pg_get_functiondef(to_regprocedure('private.persist_change_application(uuid,timestamp with time zone,text,public.change_application_source,text,text,text,date,public.change_action_kind,text,text,date,jsonb,boolean)')) like '%cancelled product change task cannot be reactivated%', false)
              and coalesce(pg_get_functiondef(to_regprocedure('private.persist_change_application(uuid,timestamp with time zone,text,public.change_application_source,text,text,text,date,public.change_action_kind,text,text,date,jsonb,boolean)')) like '%existing_action.change_application_id = v_application.id%', false)
              and coalesce(pg_get_functiondef(to_regprocedure('private.persist_change_application(uuid,timestamp with time zone,text,public.change_application_source,text,text,text,date,public.change_action_kind,text,text,date,jsonb,boolean)')) like '%existing_task.status = ''cancelled''%', false)
              and coalesce(pg_get_functiondef(to_regprocedure('private.persist_change_application(uuid,timestamp with time zone,text,public.change_application_source,text,text,text,date,public.change_action_kind,text,text,date,jsonb,boolean)')) like '%p_expected_updated_at is null%', false)
              and coalesce(pg_get_functiondef(to_regprocedure('private.persist_change_application(uuid,timestamp with time zone,text,public.change_application_source,text,text,text,date,public.change_action_kind,text,text,date,jsonb,boolean)')) like '%v_application.updated_at is distinct from p_expected_updated_at%', false)
              and coalesce(pg_get_functiondef(to_regprocedure('private.persist_change_application(uuid,timestamp with time zone,text,public.change_application_source,text,text,text,date,public.change_action_kind,text,text,date,jsonb,boolean)')) like '%change application was modified by another user%', false)
              and to_regprocedure('private.sync_change_application_archive()') is not null
              and coalesce((select prosecdef from pg_proc where oid = to_regprocedure('private.sync_change_application_archive()')), false)
              and coalesce((select exists (select 1 from unnest(proconfig) cfg where cfg in ('search_path=' || chr(34) || chr(34), 'search_path=')) from pg_proc where oid = to_regprocedure('private.sync_change_application_archive()')), false)
              and not coalesce(has_function_privilege('authenticated', to_regprocedure('private.sync_change_application_archive()'), 'EXECUTE'), true)
              and not coalesce(has_function_privilege('anon', to_regprocedure('private.sync_change_application_archive()'), 'EXECUTE'), true)
              and coalesce(pg_get_functiondef(to_regprocedure('private.sync_change_application_archive()')) like '%new.status in (''completed'', ''not_applicable'')%', false)
              and coalesce(pg_get_functiondef(to_regprocedure('private.sync_change_application_archive()')) like '%new.status = ''pending''%', false)
              and exists (select 1 from pg_trigger where tgrelid = to_regclass('public.change_applications') and tgname = 'change_applications_private_audit' and tgfoid = to_regprocedure('private.record_mutation_audit()') and tgenabled in ('O', 'A'))
              and exists (select 1 from pg_trigger where tgrelid = to_regclass('public.change_action_items') and tgname = 'change_action_items_private_audit' and tgfoid = to_regprocedure('private.record_mutation_audit()') and tgenabled in ('O', 'A'))
              and exists (select 1 from pg_trigger where tgrelid = to_regclass('public.product_change_tasks') and tgname = 'product_change_tasks_private_audit' and tgfoid = to_regprocedure('private.record_mutation_audit()') and tgenabled in ('O', 'A'))
              and exists (select 1 from pg_trigger where tgrelid = to_regclass('public.product_change_tasks') and tgname = 'product_change_tasks_sync_application_archive' and tgfoid = to_regprocedure('private.sync_change_application_archive()') and tgenabled in ('O', 'A'))")
          if [ "$change_applications_ready" != "t" ]; then
            echo "::error::Production DB change application schema/RLS/grants/RPC/audit contract is not ready"
            exit 1
          fi

      - name: Verify Supabase public configuration
        env:
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
        run: |
          settings_file=$(mktemp)
          trap 'rm -f "$settings_file"' EXIT
          curl --fail --silent --show-error --max-time 20 \
            --header "apikey: $VITE_SUPABASE_ANON_KEY" \
            "${VITE_SUPABASE_URL%/}/auth/v1/settings" \
            -o "$settings_file"
          jq -e '
            type == "object"
            and .disable_signup == true
            and .mailer_autoconfirm == false
            and .external.email == true
            and .external.anonymous_users == false
          ' "$settings_file" >/dev/null || {
            echo "::error::Supabase Auth settings drifted: require signup off, email confirmation on, anonymous off, email provider on"
            exit 1
          }

      - name: Build
        run: npm run build
        env:
          VITE_APP_MODE: production
          VITE_SUPABASE_URL: ${{ env.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}

      - name: Deploy to Cloudflare Workers
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          npx --no-install wrangler deploy --name "$WORKER_NAME" --assets dist --keep-vars --compatibility-date 2026-07-04
          {
            echo "## Deploy succeeded"
            echo ""
            echo "Worker \`$WORKER_NAME\` was deployed to Cloudflare."
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Verify deployed Worker health
        run: |
          body_file=$(mktemp)
          headers_file=$(mktemp)
          trap 'rm -f "$body_file" "$headers_file"' EXIT

          curl --fail --silent --show-error --location \
            --retry 5 --retry-delay 5 --retry-all-errors --max-time 20 \
            "$WORKER_URL" -o "$body_file"
          grep -Fq 'id="root"' "$body_file" || {
            echo "::error::Worker health check failed: root mount was not found"
            exit 1
          }

          curl --fail --silent --show-error --location --head \
            --retry 5 --retry-delay 5 --retry-all-errors --max-time 20 \
            "$WORKER_URL" -o "$headers_file"
          grep -Eqi '^content-security-policy:' "$headers_file" || {
            echo "::error::Worker health check failed: Content-Security-Policy header is missing"
            exit 1
          }
          grep -Eqi '^x-content-type-options:[[:space:]]*nosniff' "$headers_file" || {
            echo "::error::Worker health check failed: X-Content-Type-Options nosniff is missing"
            exit 1
          }

          echo "- post-deploy health check: root mount and security headers verified" >> "$GITHUB_STEP_SUMMARY"
