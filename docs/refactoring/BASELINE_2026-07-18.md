# Refactoring baseline — 2026-07-18

이 문서는 교정 리팩토링이 보존해야 할 기준 커밋과 품질 계약을 고정한다.

## 기준 커밋

- repository: `NKH-92/SQA-P1-Workflow`
- commit: `e6ce717648f4c82cd70c85a4f7bd7510c9a114e3`
- 기준 브랜치: `main`
- 기존 실행 기록: [`../REFACTORING_PLAN.md`](../REFACTORING_PLAN.md)

## 보존 계약

- UI 문구, DOM/ARIA, hash route, keyboard/IME 동작을 변경하지 않는다.
- local/remote command 결과와 오류 의미를 변경하지 않는다.
- Supabase RLS/RPC/ACL/audit/Storage 계약을 약화하지 않는다.
- 기존 migration은 append-only로 취급하며 수정, 삭제, rename하지 않는다.
- 배포 순서는 `Backup DB -> DB Migrate -> Deploy Worker`를 유지한다.
- 의도된 동작 변경은 review read-state parity와 announcement sort 회귀 수정뿐이다.

## 기준 품질 게이트

2026-07-19에 위 커밋을 clean worktree에서 재검증했다.

| Gate | 결과 |
|---|---|
| Node | `v24.16.0` |
| npm | `11.13.0` |
| Supabase CLI | `2.109.1` |
| `npm ci` | pass, 0 vulnerabilities |
| `npm run typecheck` | pass |
| `npm run lint` | pass |
| `npm test -- --run` | pass |
| `npm run build` | pass |
| bundle budget | pass, 2 JS chunks / 111086 gzip bytes |
| `npm run test:rls` | blocked: Docker/local Supabase unavailable, 8 suites / 0 tests |

RLS가 skip 또는 실행 전 실패한 상태는 merge-ready가 아니다. OPS-RLS가 로컬 환경과 공용 실행기를 복구한 뒤 이 기준선에 성공 증거를 추가한다.
